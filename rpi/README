Ok, so this is the RPI version of the sensor.

Mic: run through the MCP3002. The gain is turned up by removing the guard sensor on the Sparkfun Sound Detector (r3) and adding a 100k resistor (r17)

DHT22: run directly into the RPI

Light Sensor:

 TSL235R, connected on GPIO7

